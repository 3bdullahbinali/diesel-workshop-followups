# نشر الواجهة الموثقة

الهدف: يقرأ الفريق السجل ويعدّله من الموقع **دون** مشاركة ملف الشيت بالرابط.
كل طلب يمر على تحقق الجلسة قبل أن يعيد صفاً أو يكتب خلية.

## قبل البدء

`الشيت` هو **متابعات فريق صيانة المحطات الخارجية | External Stations**.
لا تغيّر مشاركته. يبقى خاصاً بك، والواجهة هي من يقرأ نيابة عن الفريق.

## ١ — إنشاء المشروع

**اجعله مشروعاً مستقلاً، لا مرتبطاً بالشيت.**

1. افتح [script.google.com](https://script.google.com) ← **مشروع جديد**.
2. سمّه: `واجهة المحطات الخارجية`.
3. احذف محتوى `Code.gs`، والصق محتوى `stations-api.gs` مكانه.
4. تأكد أن `CONFIG.spreadsheetId` يطابق معرّف الشيت (مضبوط مسبقاً) — المشروع
   المستقل يفتح الملف بهذا المعرّف، فلا يعمل بدونه.
5. احفظ.

### لماذا مستقل

رابط `/exec` محفوظ في `api-config.json`، وثباته يعني ثبات الموقع. السكربت المرتبط
بالشيت (عبر الإضافات ← Apps Script) يُنسخ مع الملف ويُحذف بحذفه؛ فإن أُعيد بناء الشيت
أو نُسخ، ظهرت نسخة ثانية برابط مختلف وانكسر الربط.

والمشروع المستقل ملف قائم بذاته في Drive: يُنقل إلى مساحة مشتركة أو تُنقل ملكيته
للفريق دون المساس بالشيت.

**لا تدمجه مع سكربت ورشة الديزل.** مشروعان منفصلان بأوراق مستخدمين وجلسات وسجلات
وصول مستقلة؛ الدمج يجعل صلاحية في أحدهما تمس بيانات الآخر.

يعمل السكربت مرتبطاً بالشيت أيضاً إن اخترت ذلك — `book()` يرجع إلى الملف المرتبط حين
لا يجد معرّفاً — لكن المستقل هو الموصى به.

## ٢ — إنشاء الأوراق والحساب الأول

1. من قائمة الدوال اختر `setup` ← **تشغيل**. وافق على الأذونات عند طلبها
   (المشروع المستقل يطلب إذن الوصول إلى جداول بياناتك في أول تشغيل).
   تُنشأ ثلاث أوراق: `المستخدمون` و`الجلسات` مخفيتان، و`سجل الوصول` ظاهرة.
2. افتح `addUser` وعدّل الاسم والصلاحية وكلمة المرور المؤقتة.
3. شغّل `addUser`.
4. **امسح كلمة المرور من الملف واحفظ.** لا تتركها مكتوبة في السكربت.

الصلاحيات: `viewer` قراءة فقط · `editor` قراءة وتحديث · `admin` إدارة كاملة.
كلمة المرور تُحفظ مُلَحّة ومُجزّأة ٤٠٠٠ دورة SHA-256، ولا تُخزَّن كنص.

## ٣ — النشر

**نشر** ← **عملية نشر جديدة** ← النوع: **تطبيق ويب**:

| الحقل | القيمة |
|---|---|
| التنفيذ باسم | **أنا** |
| من له حق الوصول | **أي شخص** |

«أي شخص» هنا تعني أن الرابط يستقبل الطلب، **لا** أن البيانات مكشوفة. أول ما يفعله
`handle` هو `requireUser(token)` قبل أي قراءة. من يفتح الرابط في المتصفح يرى فحص صحة
فقط: اسم الخدمة وعدد الصفوف في كل تبويب، بلا أي بيانات.

لو اخترت «أي شخص داخل المؤسسة»، سيطلب Google تسجيل دخول قبل وصول الطلب، وسيفشل
الاتصال من الموقع برسالة «رد غير متوقع من الخادم».

## ٤ — ربط الموقع

انسخ رابط `/exec` وضعه في `stations/api-config.json`:

```json
{ "apiUrl": "https://script.google.com/macros/s/AKfyc…/exec", "refreshSeconds": 120 }
```

ثم `python3 stations/tools/build-preview.py` ليُولَّد `api-config.js` منه.

## ٥ — التحقق

1. افتح رابط `/exec` في المتصفح: يجب أن يظهر `"ok": true` مع `"ready": true`
   وعدد صفوف كل تبويب. لو ظهر `"ready": false` فالحساب لم يُضَف بعد.
2. افتح الموقع: يظهر شريط «المصدر: النسخة المضمّنة» مع زر **تسجيل الدخول**.
3. سجّل الدخول: يتحول الشريط إلى «المصدر: الشيت عبر واجهة موثقة — اسمك»، ويذكر
   بعده «تحرير» أو «قراءة فقط» بحسب صلاحيتك.
4. بصلاحية `editor` أو `admin`: عدّل متابعة واحفظ، ثم افتح الشيت وتأكد أن القيمة
   تغيّرت وأن `Updated_At` و`Updated_By` كُتبا.
5. راجع ورقة **سجل الوصول**: كل دخول وقراءة وكتابة مسجّلة بالوقت والمستخدم.

## الصلاحيات والكتابة

| الصلاحية | القراءة | التعديل والإضافة والإغلاق |
|---|---|---|
| `viewer` | نعم | لا |
| `editor` | نعم | نعم |
| `admin` | نعم | نعم |

الواجهة تكتب في تبويبَي `Followups` و`Correspondence` فقط، وفي الأعمدة التي يملكها
الموقع. أعمدة المعادلات (`Closure_Check`, `Due_Alert`, `Data_Check`, `Stage_Label`)
والمعرّف لا تُكتب أبداً. `Updated_At` و`Updated_By` يكتبهما الخادم لا المتصفح.

التبويبات الأخرى — الأنشطة والمحطات والمصادر وبنود الشراء — للقراءة فقط من الموقع،
وتُحدَّث في الشيت مباشرة.

### قواعد يفرضها الخادم لا المتصفح

الإغلاق يتطلب تاريخاً **ودليلاً** معاً؛ يُرفض بدونهما حتى لو استُدعيت الواجهة مباشرة.
اتجاه الكتاب يُثبت عند الإنشاء ولا يقبل التغيير؛ الرد يُسجَّل كتاباً آخر مرتبطاً.
الموضوع والحالة مطلوبان في المتابعة، والموضوع والرقم في الكتاب.

### منع الكتابة فوق تعديل غيرك

كل تعديل يرسل `Updated_At` الذي حُمِّل مع الصف. اختلافه عن المحفوظ يعني أن شخصاً آخر
عدّل السجل بعد تحميلك له، فيُرفض الحفظ برسالة تذكر وقت التعديل الآخر وتطلب التحديث.
لا يُكتب فوق عمل أحد بصمت.

## الحدود المقصودة

الواجهة لا تحذف شيئاً. لا يوجد إجراء حذف أصلاً، لأي صلاحية.

الجلسة ١٢ ساعة، ورمزها يُحفظ في `sessionStorage` فينتهي بإغلاق التبويب.
تُخزَّن **بصمة** الرمز في الورقة لا الرمز نفسه، فتسريب الورقة لا يمنح جلسة صالحة.

خمس محاولات فاشلة توقف الحساب ١٥ دقيقة. رسالة الرفض واحدة للاسم الخاطئ ولكلمة المرور
الخاطئة، حتى لا يُستدل على الحسابات الموجودة.

## ما لا تفعله

لا تشارك الشيت بالرابط بعد تفعيل الواجهة؛ يُفرغ ذلك التوثيق من معناه.
لا تُفعّل `allowPublicRead` في `sheets-config.json` إلا إذا قررت الجهة أن القراءة
العامة مقبولة — وهي تعني أن أي حامل للرابط يقرأ البيانات دون تسجيل دخول.
لا تترك كلمة مرور في `addUser` بعد تشغيلها.
